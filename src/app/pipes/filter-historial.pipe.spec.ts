import { FilterHistorialPipe } from './filter-historial.pipe';

describe('FilterHistorialPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHistorialPipe();
    expect(pipe).toBeTruthy();
  });
});

import { CashBoxTwoPipe } from './cash-box-two.pipe';

describe('CashBoxTwoPipe', () => {
  it('create an instance', () => {
    const pipe = new CashBoxTwoPipe();
    expect(pipe).toBeTruthy();
  });
});

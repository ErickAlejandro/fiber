import { FilterVlansPipe } from './filter-vlans.pipe';

describe('FilterVlansPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterVlansPipe();
    expect(pipe).toBeTruthy();
  });
});

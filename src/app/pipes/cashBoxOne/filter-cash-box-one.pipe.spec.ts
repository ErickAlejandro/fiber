import { FilterCashBoxOnePipe } from './filter-cash-box-one.pipe';

describe('FilterCashBoxOnePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCashBoxOnePipe();
    expect(pipe).toBeTruthy();
  });
});
